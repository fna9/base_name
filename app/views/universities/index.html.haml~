%h1 Университеты

%table
  %thead
    %tr
      %th Название
      %th
      %th
      %th

  %tbody
    - @universities.each do |university|
      %tr
        %td= university.name
        %td
          = link_to university, class: "btn btn-success btn-xs" do
            =fa_icon "arrow-circle-right"
            Показать

        %td
          = link_to edit_university_path(university), class: "btn btn-warning btn-xs" do
            =fa_icon "edit"
            Редактировать

        %td
          = link_to university, :method => :delete, :data => { :confirm => 'Вы уверены?' },class: "btn btn-danger btn-xs" do
            =fa_icon "remove"
            Удалить

%br

= link_to 'Создать новый', new_university_path, "data-toggle" => "tooltip", :title => "Добавить новый университет", class: "btn btn-primary btn-xs"


